//host.json file in azure functionApp (webapp)
//replace the block with sampling enabled:
//note this can go after another block ending in },

{
    "logging": {
        "logLevel": {
        "default": "Trace"
    },
    "fileLoggingMode": "always"
},
    "extensionBundle": {
      "id": "Microsoft.Azure.Functions.ExtensionBundle",
      "version": "[1.*, 2.0.0)"
    }
  }